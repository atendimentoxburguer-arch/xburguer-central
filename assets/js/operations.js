/* X Burguer Gestor PRO V10 — operations */
function renderEntregas(){const del=state.orders.filter(o=>o.type==='Delivery'&&!['analysis','cancelled'].includes(o.status)&&o.status!=='done');const root=document.getElementById('entregas');root.innerHTML=`<div class="page-head"><div><h1>Entregas</h1><p>Distribuição de pedidos, entregadores e acompanhamento operacional.</p></div><button class="btn btn-blue" onclick="addCourier()">${icon('person-plus')}<span>Entregador</span></button></div><div class="cards4 grid"><div class="card metric"><div class="lab">Aguardando saída</div><div class="val">${del.filter(o=>o.status==='ready'&&!o.courier).length}</div></div><div class="card metric"><div class="lab">Em rota</div><div class="val">${del.filter(o=>o.status==='ready'&&o.courier).length}</div></div><div class="card metric"><div class="lab">Entregadores ativos</div><div class="val">${state.couriers.filter(d=>d.active).length}</div></div><div class="card metric"><div class="lab">Taxa padrão</div><div class="val">${money(state.settings.deliveryFee)}</div></div></div><div class="delivery-grid"><div class="delivery-list">${del.map(o=>`<div class="delivery-card"><div><b>#${o.id} • ${esc(o.customer)}</b><div class="muted delivery-address">${icon('geo-alt')}<span>${esc(o.address)}</span></div></div><div><label class="muted">Entregador</label><select class="select" onchange="assignCourier('${o.id}',this.value)"><option value="">Aguardando</option>${state.couriers.filter(d=>d.active).map(d=>`<option value="${d.id}" ${o.courier===d.id?'selected':''}>${esc(d.name)}</option>`).join('')}</select></div><button class="btn btn-green btn-sm" onclick="advanceOrder('${o.id}')">${o.courier?'Entregue':'Finalizar'}</button></div>`).join('')||'<div class="empty">Nenhuma entrega ativa.</div>'}</div><div class="card"><h3 style="margin-top:0">Entregadores</h3>${state.couriers.map(d=>`<div class="courier"><div class="avatar">${esc(d.name.slice(0,2).toUpperCase())}</div><div style="flex:1"><b>${esc(d.name)}</b><div class="muted">${esc(d.vehicle)} • ${d.deliveries} entregas</div></div><span class="badge ${d.active?'b-green':'b-gray'}">${d.active?'Ativo':'Inativo'}</span></div>`).join('')}</div></div>`}
function assignCourier(oid,did){state.orders.find(o=>o.id===oid).courier=did;save();toast('Entregador atribuído.')}
function addCourier(){const n=prompt('Nome do entregador:');if(!n)return;const phone=prompt('Telefone:',''),vehicle=prompt('Veículo (Motocicleta/Carro/Bicicleta):','Motocicleta')||'Motocicleta';state.couriers.push({id:uid('d'),name:n,phone,vehicle,active:true,deliveries:0});save()}
function renderPerformance(){const done=state.orders.filter(o=>o.status==='done');const revenue=done.reduce((s,o)=>s+orderTotal(o),0),ticket=done.length?revenue/done.length:0;const top={};done.forEach(o=>o.items.forEach(i=>{const n=product(i.p)?.name||'Item';top[n]=(top[n]||0)+i.q}));const tops=Object.entries(top).sort((a,b)=>b[1]-a[1]).slice(0,5);document.getElementById('performance').innerHTML=`<div class="page-head"><div><h1>Meu desempenho</h1><p>Visão de vendas, canais, produtos e operação.</p></div><select class="select"><option>Hoje</option><option>Últimos 7 dias</option><option>Este mês</option></select></div><div class="cards4 grid"><div class="card metric"><div class="lab">Faturamento</div><div class="val">${money(revenue)}</div><div class="sub">Pedidos finalizados</div></div><div class="card metric"><div class="lab">Pedidos</div><div class="val">${done.length}</div></div><div class="card metric"><div class="lab">Ticket médio</div><div class="val">${money(ticket)}</div></div><div class="card metric"><div class="lab">Tempo médio produção</div><div class="val">18 min</div></div></div><div class="split2"><div class="card"><h3 style="margin-top:0">Vendas por horário</h3><div class="chart">${[25,38,56,82,67,92,78,45].map((v,i)=>`<div class="bar" style="height:${v}%"><span>${16+i}h</span></div>`).join('')}</div></div><div class="card"><h3 style="margin-top:0">Mais vendidos</h3>${tops.map(([n,q],i)=>`<div class="list-row"><span>${i+1}. ${esc(n)}</span><b>${q} un.</b></div>`).join('')||'<div class="empty">Finalize pedidos para gerar ranking.</div>'}</div></div><div class="card" style="margin-top:12px"><h3 style="margin-top:0">Canais</h3>${['Delivery','Mesa','Balcão','Retirada'].map(t=>{const n=state.orders.filter(o=>o.type===t).length,p=state.orders.length?Math.round(n/state.orders.length*100):0;return `<div style="margin:10px 0"><div class="order-row"><span>${t}</span><b>${n} pedidos</b></div><div class="progress"><i style="width:${p}%"></i></div></div>`}).join('')}</div>`}
function renderKds(){const qs=state.orders.filter(o=>o.status==='production');document.getElementById('kds').innerHTML=`<div class="page-head"><div><h1>Cozinha - KDS</h1><p>Display de produção com tempo e observações.</p></div><button class="btn btn-outline" onclick="document.documentElement.requestFullscreen?.()">${icon('arrows-fullscreen')}<span>Tela cheia</span></button></div><div class="toolbar"><div class="toolbar-left"><span class="badge b-gray">Todos os setores</span><span class="badge b-orange">${qs.length} em produção</span></div><div class="muted">Atualização automática local</div></div><div class="kds-grid">${qs.map(o=>`<div class="k-ticket ${orderAge(o)>35?'late':''}"><header><span>${o.table||'#'+o.id} • ${o.type}</span><span>${orderAge(o)} min</span></header><div class="k-body">${o.items.map(i=>`<div class="item"><b>${i.q}x ${esc(product(i.p)?.name||'Item')}</b></div>`).join('')}${o.notes?`<p class="warning">Obs.: ${esc(o.notes)}</p>`:''}<button class="btn btn-green" style="width:100%;margin-top:9px" onclick="advanceOrder('${o.id}')">Marcar como pronto</button></div></div>`).join('')||'<div class="empty">Nenhum pedido em produção.</div>'}</div>`}
